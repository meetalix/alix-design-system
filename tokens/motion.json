{
  "duration": {
    "$type": "duration",
    "$description": "Milliseconds. Fades and small position shifts only — never bounce, never overshoot.",
    "fast": { "$value": 120, "$description": "Taps, toggles" },
    "base": { "$value": 180, "$description": "Most transitions" },
    "slow": { "$value": 260, "$description": "Sheets, screens" }
  },
  "easing": {
    "$type": "cubicBezier",
    "default": { "$value": [0.2, 0.8, 0.2, 1], "$description": "Gentle ease-out — the only easing curve. RN: Easing.bezier(0.2, 0.8, 0.2, 1)" }
  }
}
